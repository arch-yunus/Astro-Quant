version: '3.8'

services:
  astro-quant-dashboard:
    build: .
    container_name: astro_quant_live
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
